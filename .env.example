# Tulare County AI Chatbot — Environment Variables
# Copy to .env and fill in values

# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Database password (defaults to "localdev" in docker-compose)
DB_PASSWORD=localdev

# Admin dashboard password (defaults to "demo2026")
ADMIN_PASSWORD=demo2026

# Frontend API URL (set for deployed environments)
# NEXT_PUBLIC_API_URL=https://your-backend-url.onrender.com

# Optional: Base path for frontend (e.g., /tulare-chatbot)
# NEXT_PUBLIC_BASE_PATH=
